#include <stdio.h>
int main(void) {
    int t, n, x;
    scanf("%d", & t);
    while (t--)
    {
        scanf("%d %d", & n, & x);
        if (x%n==0)
        {
            printf("yes\n");
        }
        else
        {
            printf("no\n");
        }
    }
}
